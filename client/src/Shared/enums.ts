export enum Place {
  lobby = 'lobby',
  game = 'game',
}

export enum SizeCard {
  small = 'small',
}

export enum Issue {
  update = 'update',
  delete = 'delete',
  create = 'create',
}

export enum Priority {
  low = 'Low',
  middle = 'Middle',
  hight = 'Hight',
}
