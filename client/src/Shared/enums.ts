export enum Place {
  lobby = 'lobby',
  game = 'game',
}

export enum SizeCard {
  small = 'small',
}

export enum Issue {
  update = 'update',
  delete = 'delete',
  create = 'create',
  game = 'game',
}

export enum Priority {
  low = 'low',
  middle = 'middle',
  hight = 'hight',
}

export enum Decks {
  fibonacci = 'fibonacci',
  modifiedFibonacci = 'modifiedFibonacci',
  tshirts = 'tshirts',
  powers = 'powers',
}

export enum TimerStatus {
  STARTED = 'Started',
  STOPPED = 'Stopped',
}
